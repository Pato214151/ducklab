/** Página /planes (el contenido está en PlanesContent). */

import PlanesContent from './PlanesContent';

export const metadata = {
  title: 'Pricing | Ducklab',
  description: 'Clear pricing for websites, POS systems and custom apps. Monthly maintenance plans starting at $79.',
};

export default function Planes() {
  return <PlanesContent />;
}
